import { StyleSheet } from "react-native";
import tw from "tailwind-react-native-classnames";

export const styles = StyleSheet.create({
  textBlue: tw`text-blue-600`,
  appContainer: tw`h-full w-full bg-white`,
});
